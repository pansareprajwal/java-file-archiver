# java-file-archiver
A Java-based system utility for archiving multiple text files into a single, unified bundle, featuring custom byte-aligned metadata headers and XOR stream encryption for data security.
